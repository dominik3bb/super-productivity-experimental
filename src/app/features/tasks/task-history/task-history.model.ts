/** * Per-task version history entry (discussion #6620). Nested on `Task.revisions` * so it syncs as a normal TASK update — no new entity type, no schema bump. */export type TaskRevisionKind =  | 'created'  | 'title'  | 'notes'  | 'subtaskAdded'  | 'subtaskRemoved'  | 'subtaskRenamed';export interface TaskRevisionCopy {  id: string;  created: number;  kind: TaskRevisionKind;  /**   * For `created`: the title at creation.   * For `title`: the previous title (restore target).   */  title?: string;  /** Previous notes when `kind === 'notes'`. */  notes?: string;  /** Child task id for subtask events. */  subTaskId?: string;  /** Child title at the time of the event (new title when renamed). */  subTaskTitle?: string;  /** Previous child title when `kind === 'subtaskRenamed'`. */  previousSubTaskTitle?: string;}export type TaskRevision = Readonly<TaskRevisionCopy>;